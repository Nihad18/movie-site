export type FooterLinksData = {
    title: string;
    links: { title: string; url: string }[];
};

export type OrderFormData = {
    nameSurname:string;
    email:string;
    cellPhone:string;
    payment:string;
}